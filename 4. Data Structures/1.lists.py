letters = ["a", "b", "c"]
matrix = [[1, 2], [3, 4]]
zeros = [0] * 5  # total 5 zeros

# in python every object of the list can be in differnet types
combined = zeros + letters
print(combined)

numbers = list(range(20))  # 20 is not included
print(numbers)  # 0 to 19

chars = list("Hello World")
print(chars)
print(len(chars))
